name: Mobile App CI

on:
  push:
    paths:
      - "mobile-app/**"
      - ".github/workflows/mobile-ci.yml"
  pull_request:
    paths:
      - "mobile-app/**"
      - ".github/workflows/mobile-ci.yml"

jobs:
  install-and-check:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: mobile-app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "mobile-app/package-lock.json"

      - name: Install dependencies
        run: npm install

      # Optional later when we add scripts
      # - name: Run lint
      #   run: npm run lint

      # - name: Run tests
      #   run: npm test
